html
	head
		title facebook callback
	body
		script
			var code = location.search.substring(6);
			var postedMessages = [];
			var auth = {
				command:"signin",
				auth: {
					facebook:{
						code: code
					}
				}
			};
			window.opener.postMessage(auth,"*")
			window.close();